server:
  port: 8000

eureka:
  client:
    fetch-registry: true  # ??? ????? ???
    register-with-eureka: true  # ??? ????? ???
    service-url:
      defaultZone: http://localhost:8761/eureka # ??? ?????? ??

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes: # ???? ?? ??
        - id: user-service  # ?? ???? ??? ???? ??? ? ???? ?? ??

          # ?? ???? ??? ??? ?? ???? ??
          uri: http://localhost:8080 # ???? ??, http://localhost:8000/user ? ???? http://localhost:0 ? ???

          # ????? ?? ??? predicates? ?? ???? ???? route? ??? ?? ??
          predicates:
            - Path=/user/** # ?? gateway ??? /user/**? ???? ?? user-service? ?????? ??

        - id: order-service
          uri: http://localhost:8081 # ??? ? ??, http://localhost:8000/order ? ???? http://localhost:1 ? ???
          predicates:
            - Path=/order/** # ?? gateway ??? /order/**? ???? ?? order-service? ?????? ??

# localhost:8000/user/info -> localhost:0/user/info
# localhost:8000/order/info -> localhost:1/order/info